import { SCurrencyPipe } from './s-currency.pipe';

describe('SCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
